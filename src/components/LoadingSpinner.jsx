import React from "react";

const LoadingSpinner = () => {
	return (
		<div className="flex items-center justify-center space-x-2">
			<span className="justify-center">Loading...</span>
		</div>
	);
};

export default LoadingSpinner;
